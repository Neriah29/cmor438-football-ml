[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "football_ml"
version = "0.1.0"
description = "Custom ML package for CMOR 438 using international football data"
requires-python = ">=3.8"
